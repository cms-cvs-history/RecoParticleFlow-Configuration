process ANALYSIS = {

    untracked PSet maxEvents = {untracked int32 input = -1}

    source = PoolSource {
        untracked vstring fileNames = { 'file:aod.root' }
    }

    module pfCandidateAnalyzer = PFCandidateAnalyzer {
        InputTag PFCandidates = particleFlow 
        untracked bool verbose = true
    }
    path p = { 
        pfCandidateAnalyzer
    }
}
 
